CREATE TABLE tickets (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(100),
    issue_description TEXT,
    status VARCHAR(50)
);


CREATE TABLE tickets (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(100),
    issue_description TEXT,
    status VARCHAR(50)
);

SELECT * FROM tickets;
DESCRIBE tickets;